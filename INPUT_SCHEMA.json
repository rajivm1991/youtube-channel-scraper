{
    "title": "Input schema for the youtube-channel-scraper actor",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "title": "Start URL",
            "type": "string",
            "description": "Youtube Channel URL - Videos Tab - Uploads Section",
            "prefill": "https://www.youtube.com/user/Maroon5/videos?view=0&sort=dd&shelf_id=0",
            "editor": "textfield",
            "nullable": false
        },
        "maxResults": {
            "title": "Maximum videos",
            "type": "integer",
            "description": "Limit number of videos to crawl; <br />maximum: 999",
            "minimum": 1,
            "maximum": 999,
            "default": 10,
            "unit": "Videos",
            "nullable": true
        },
        "proxyConfiguration": {
            "title": "Proxy configuration",
            "type": "object",
            "editor": "proxy",
            "description": "The best option is usually Automatic proxy. But you can also use your own proxies or no proxy",
            "prefill": {
                "useApifyProxy": true
            },
            "sectionCaption": "Advanced options"
        },
        "verboseLog": {
            "title": "Verbose log",
            "type": "boolean",
            "description": "Debug messages will be included in the log.",
            "default": true
        }
    },
    "required": [
        "startUrl"
    ]
}
